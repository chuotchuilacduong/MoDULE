name: pacs_repselect_domain_baseline
output_root: runs
learn:
  seed: 42
  dataset: pacs
  data_dir: ./dataset/data_folder/pacs
  unlearn_setting: random
  forget_ratio: 0.1
  model_name: module_small_patch16_224
  pretrained: true
  moe_layers: FFFFFFFFFFSS
  num_experts: 8
  gate_k: 2
  expert_depth: 2
  expert_hidden_ratio: 2
  epochs: 100
  batch_size: 128
  lr: 0.0005
  weight_decay: 0.05
  lambda_sparse: 0.5
  lambda_balance: 0.5
  lambda_div: 0.5
  ema_alpha: 0.9
  wandb_mode: disabled
unlearn:
  unlearn_algo: rep_select
  unlearn_setting: domain
  forget_domains:
  - 3
  study_name: RepSelect__PACS__domain__pacs_repselect_domain__seed42
  epochs: 20
  batch_size: 128
  fa_threshold: 0.1
  lr: 0.0001
  weight_decay: 0.0
  rep_select_lr: 2.0
  k_pcs: 3
  use_lora_adversary: false
  lora_rank: 8
  lora_lr: 0.05
  wandb_mode: online
  wandb_group: MainTable_PACS_seed42
  wandb_tags:
  - main_table
  - PACS
  - domain
  - RepSelect
  - seed42
  baseline_name: RepSelect
  dataset: pacs
  scenario: domain
  config_name: pacs_repselect_domain
  config_path: config/experiments/pacs_repselect_domain.yaml
  git_sha: de4238adaca520fc69b1be448f369e20f6783dba
  learning_rate: 0.0001
  dataset_name: PACS
  _note_config: 'k_pcs=3, rep_select_lr(alpha)=2.0, LoRA adversary off -- chon tu
    sweep2 (class: FA 10.21 RA 91.15 TA 84.50 MIA 0.5444). k_pcs nho => S_tilde ->
    1 => correction Mahalanobis gan nhu bi vo hieu, RepSelect moi thuc su quen.'
